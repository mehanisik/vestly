{
  "name": "@vestly/api",
  "version": "0.0.0",
  "private": true,
  "main": "index.ts",
  "scripts": {
    "dev": "PORT=3001 BETTER_AUTH_URL=http://localhost:3001 bun --env-file=../../.env run --hot index.ts",
    "build": "bun x tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.6.16",
    "@vestly/db": "workspace:*",
    "@vestly/auth": "workspace:*"
  }
}
